# Seed Data

Victory Garden ships with a small default Rails seed set for first-run development and Pi installs.

Source:

- [`../ruby_service/db/seeds.rb`](/Users/noel/coding/python/victory_garden/ruby_service/db/seeds.rb)

## Seeded Crop Profiles

### Tomato

- `crop_id`: `tomato`
- `dry_threshold`: `30.0`
- `max_pulse_runtime_sec`: `45`
- `daily_max_runtime_sec`: `300`

### Basil

- `crop_id`: `basil`
- `dry_threshold`: `40.0`
- `max_pulse_runtime_sec`: `30`
- `daily_max_runtime_sec`: `240`

## Seeded Zones

### Zone 1

- `zone_id`: `zone1`
- `name`: `Greenhouse Zone 1`
- crop: `tomato`
- active: `true`

### Zone 2

- `zone_id`: `zone2`
- `name`: `Greenhouse Zone 2`
- crop: `basil`
- active: `true`

## Seeded Connection Settings

The default connection settings seed assumes the broker runs locally on the Pi:

- `mqtt_host`: `localhost`
- `mqtt_port`: `1883`
- `mqtt_username`: blank by default for local development
- `mqtt_password`: blank by default for local development
- readings topic: `greenhouse/zones/+/state`
- actuators topic: `greenhouse/zones/+/actuator/status`
- command topic: `greenhouse/zones/{zone_id}/actuator/command`
- config topic: `greenhouse/system/config/current`

Production Pi installs generated by `deploy/install_pi.sh` also provision broker credentials in `/etc/victory_garden.env` and configure Mosquitto to require them.

## When Seeds Run

Seeds are applied by:

- local development setup via `./bin/dev-rails db:prepare`
- Pi install via `deploy/install_pi.sh`

## Important Note

These seeded values are starting points, not final production agronomy values.

In practice you will usually adjust:

- crop thresholds
- pulse runtime
- daily cap
- connection settings
- claimed node assignments
