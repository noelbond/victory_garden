#!/usr/bin/env bash
set -euo pipefail

APP_ROOT="$(cd "$(dirname "$0")/.." && pwd)"
RVM_BUNDLE="/Users/noel/.rvm/gems/ruby-3.4.3/wrappers/bundle"

if [[ ! -x "$RVM_BUNDLE" ]]; then
  echo "Missing bundle wrapper: $RVM_BUNDLE" >&2
  exit 1
fi

cd "$APP_ROOT"
exec "$RVM_BUNDLE" "$@"
